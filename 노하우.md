# 일본어 학습 앱 제작 노하우 — 기획부터 배포까지

> 💡 **핵심 한 줄: "화면을 먼저 완성하고, 데이터를 설계하고, 기능은 하나씩 — 확인할 때마다 저장."**

---

## 1. 제작 순서 (이 순서만 지켜도 절반은 편해짐)

| 단계 | 할 일 | 지킬 때의 이점 |
|---|---|---|
| ① 기획 | 화면 목록, 핵심 기능 5개 이내 정의 (문서로!) | 버튼 추가/삭제 반복, HTML 깨짐 방지 |
| ② 데이터 설계 | 문장 데이터의 형태를 먼저 확정 (id, blocks, role 등) | "이 필드 뭐였지?" 반복 수정 방지 |
| ③ 목업 | HTML+CSS만으로 화면 1장을 먼저 완성 | 태그 짝 오류, CSS 로드 문제 사전 차단 |
| ④ MVP | 카드 1장 + 다음 버튼만 되는 최소 버전 | 데이터 없이도 로직 검증 가능 |
| ⑤ 기능 추가 | 하나씩 추가 → 확인 → 저장 → 다음 기능 | 원인 추적이 쉬워짐 |
| ⑥ 배포 | 마지막에 배포 | — |

---

## 2. 이번에 배운 교훈 → 다음엔 이렇게

- 📄 **기능 고치기 전에 백업**
  - `app_v62.js`처럼 버전 폴더 복사 또는 Git 사용
  - 버전 꼬임의 80%는 롤백 불가에서 온다
- ✏️ **수정은 한 번에 한 기능**
  - 고치고 → 새로고침 확인 → 다음 기능
  - 동시 수정이 에러의 주원인
- 🏗️ **HTML 수정 후 구조 검증**
  - VS Code 확장「HTML tag checker」또는 F12 → Elements 탭에서 열린 태그 경고 확인
- 🔧 **공통 로직은 함수 하나로**
  - 문장 조립 규칙을 `buildDisplayText(s)` 하나로 만들고 여러 곳에서 호출
  - 규칙 변경 시 1곳만 수정
- 🖥️ **콘솔 로그를 습관으로**
  - "안 된다" → F12 Console 먼저
  - 에러 메시지의 줄 번호가 정답을 알려준다

---

## 3. 배포 방법 — 10분이면 휴대폰에서도 사용 가능

### GitHub Pages (무료, 추천)

1. github.com 가입 → 새 저장소(Repository) 생성
2. 앱 폴더 전체 업로드 (드래그&드롭)
3. Settings → Pages → Branch: `main` 선택 → Save
4. 1~2분 후 `https://아이디.github.io/저장소명/` 접속 완료

### Netlify Drop (가장 쉬움)

- netlify.com 접속 → 폴더를 드래그&드롭 → 즉시 배포 완료

> ⚠️ 배포 시 `?v=42` 같은 쿼리 문자열은 캐시 갱신에 도움됨 (file://와 달리 웹에서는 안정적으로 작동)

---

## 4. 다음 앱 기획 템플릿

```
1. 한 줄 정의: "○○을 ○○해서 ○○하는 앱"

2. 핵심 기능 (최대 5개):
   □ 기능1  □ 기능2  □ 기능3  □ 기능4  □ 기능5

3. 화면 목록: 메인 1개뿐이면 단일 페이지

4. 데이터 설계:
   | 필드 | 예시    | 용도        |
   | id   | v1_001 | 진도 저장 키 |

5. 제작 순서: HTML 뼈대 → CSS → 데이터 → 로직 → 기능 추가
```

---

## 5. .md 파일 사용법

- `.md`는 마크다운 문서 — 메모장으로 열면 기호가 보이고, 뷰어로 열면 예쁜 문서로 보임
- **VS Code 미리보기**: 파일 열고 `Ctrl + Shift + V`
- **GitHub**: `README.md`라는 이름으로 저장소에 올리면 자동으로 표시됨
